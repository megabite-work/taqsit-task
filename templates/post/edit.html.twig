{% extends 'base.html.twig' %}

{% block content %}
	<div class="container tm-mt-big tm-mb-big">
		<div class="row">
			<div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 mx-auto">
				<div class="tm-bg-primary-dark tm-block tm-block-h-auto">
					<div class="row">
						<div class="col-12 text-center">
							<h2 class="tm-block-title d-inline-block">Edit Post</h2>
							{% for message in app.flashes('error') %}
								<p class="text-danger h5">
									{{ message }}
								</p><br>
							{% endfor %}
						</div>
					</div>
					<div class="row tm-edit-product-row">
						<div class="col-xl-12 col-lg-12 col-md-12">
							<form action="{{ path('app_post_update', {id: post.id}) }}" method="POST" enctype="multipart/form-data" class="tm-edit-product-form">
								<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
								<input type="hidden" name="_method" value="PATCH">
								<div class="form-group mb-3">
									<label for="title">Title</label>
									<input id="title" name="title" type="text" class="form-control validate" required value="{{ post.title }}"/>
								</div>
								<div class="form-group mb-3">
									<label for="body">Body</label>
									<textarea id="body" name="body" class="form-control validate" required rows="8">{{ post.body }}</textarea>
								</div>
								<div class="form-group mb-3">
									<div id="post_image_preview">
										<img src="{{ asset(post.image) }}" alt="image" width="200">
									</div>
									<label for="post_image">Image</label>
									<input id="post_image" name="image" type="file" class="form-control validate"/>
								</div>
								<div>
									<button type="submit" class="btn btn-primary btn-block text-uppercase mb-3">Update</button>
								</div>
								<div>
									<a href="{{ path('app_home') }}" class="btn btn-secondary btn-block text-uppercase mb-3">Back</a>
								</div>
							</form>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
